{% extends 'base.html.twig' %}

{% block title %}Modifier {{ formProject.vars.value.name }}{% endblock %}
{% block page_title %}Modifier {{ formProject.vars.value.name }}{% endblock %}

{% block body %}
    {{ form_start(formProject) }}
        <div>
            {{ form_label(formProject.name) }}
            {{ form_widget(formProject.name) }}
        </div>
        <div>
            {{ form_label(formProject.employees) }}
            {{ form_widget(formProject.employees) }}
        </div>
        <br>
        <button class="button button-submit">Modifier</button>
    {{ form_end(formProject) }}
{% endblock %}